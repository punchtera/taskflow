# --- .NET ---
bin/
obj/
*.user
*.suo
.vs/
[Dd]ebug/
[Rr]elease/
artifacts/

# SQLite database files (created at runtime / seeded)
*.db
*.db-shm
*.db-wal

# --- Node / React client ---
node_modules/
client/dist/
vite.config.ts.timestamp-*

# Built SPA output served by the API (regenerated by `npm run build`)
src/TaskFlow.Api/wwwroot/assets/

# --- Editors / OS ---
.idea/
.DS_Store
*.swp

# --- Env ---
.env
.env.local

# --- Exercise brief (do not republish the company's document) ---
*.pdf
